<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sw.aportes">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"
            android:parentActivityName=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Registro"
            android:label="@string/titleRegistro"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Publicar"
            android:label="@string/title_activity_publicar" />
        <activity
            android:name=".ValorUsu"
            android:label="@string/title_activity_valorusu" />
        <activity
            android:name=".Cantidad"
            android:label="@string/title_activity_cantidad" />
        <activity
            android:name=".UsuarioS"
            android:label="@string/title_activity_usuario_s"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Perfil"
            android:label="@string/title_activity_perfil"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Ayuda"
            android:label="@string/title_activity_ayuda"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="sw.aportes.MainActivity" />
        </activity>
        <activity
            android:name=".OfertasAceptadas"
            android:label="@string/title_activity_ofertas_aceptadas"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
